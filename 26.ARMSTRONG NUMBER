#include <stdio.h>

int main()
 {
   int sum=0,temp,n,a;
printf("Enter an integer: ");
   scanf("%d", &n);
temp=n;
while(n>0)
{
	a=n%10;
	sum=sum+(a*a*a);
	n=n/10;
}
   if ((int)result == num)
    printf("%d is an Armstrong number.", num);
   else
    printf("%d is not an Armstrong number.", num);
   return 0;
}

   
